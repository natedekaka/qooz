# Qooz Environment Configuration
# Copy file ini ke .env.local dan sesuaikan nilainya

# API URL (default: localhost)
NEXT_PUBLIC_API_URL=http://localhost:8090/qooz/api

# Polling interval untuk update status (ms)
NEXT_PUBLIC_POLL_INTERVAL=2000

# Game timeout (ms)
NEXT_PUBLIC_GAME_TIMEOUT=30000
